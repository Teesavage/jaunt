export const metadata = {
    title: "Confirm Ticket Order",
    description: "Confirm your Maryland restaurant ticket order.",
  };
  
  export default function TicketCheckoutLayout({ children }) {
    return (
        <div>
            {children}
        </div>
    );
  }