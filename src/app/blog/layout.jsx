export const metadata = {
    title: "Event Blogs",
    description: "Get first-hand information, news and reviews about your favorite Locations and Events.",
  };
  
  export default function BlogHomeLayout({ children }) {
    return (
        <div>
            {children}
        </div>
    );
  }